{
  "name": "pub-bumper",
  "version": "0.0.0-pre-release.1",
  "description": "Intelligent version bumping for a related suite of modules in development",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "run": "tsc && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@jsspec/cli-options": "^0.0.8",
    "git-utils": "^5.7.3"
  }
}
